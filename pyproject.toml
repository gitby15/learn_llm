[project]
name = "learn-llm"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
     "numpy>=2.5.2",
     "torch===2.8.0",
     "tqdm>=4.70.0",
     "transformers>=5.15.0",
]

[project.scripts]
learn-llm = "learn_llm:main"
timllm = "learn_llm.model.timllm:test"

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"
